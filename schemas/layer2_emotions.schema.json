{
  "type": "object",
  "required": ["scopes"],
  "additionalProperties": false,
  "properties": {
    "scopes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["scope_id", "emotion", "intensity", "confidence", "evidence_refs"],
        "additionalProperties": false,
        "properties": {
          "scope_id": {"type": "string"},
          "emotion": {"type": "string"},
          "intensity": {"enum": ["low", "medium", "high"]},
          "confidence": {"enum": ["low", "medium", "high"]},
          "evidence_refs": {"type": "array", "items": {"type": "string"}}
        }
      }
    }
  }
}

