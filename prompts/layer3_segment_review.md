# Layer 3 â€” segment construction and final review

Review the complete unit: exactly one moderator question plus one respondent answer.

Confirm that:

- the question and answer are preserved;
- final emotions are grounded in validated Layer 2 outputs;
- the primary emotion is selected from the final emotion list;
- ambiguity is recorded in review notes rather than hidden.

Return JSON matching `schemas/layer3_segment_review.schema.json`.

